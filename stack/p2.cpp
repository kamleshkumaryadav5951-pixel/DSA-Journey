#include<iostream>
#include<string>
using namespace std;
int main(){
    string var = "([{}])";
    for(int i =0 ; var.size();i++){
        int mid = var.size()/2;
        if(mid ==)
    }
}
(s[i] = ')' && s.top() = '(')